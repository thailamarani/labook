export interface UserDB {
    id: string,
    name: string,
    email: string,
    created_at: string
};

export interface UserDBPost {
    id: string,
    name: string,
    email: string,
    created_at: string
};